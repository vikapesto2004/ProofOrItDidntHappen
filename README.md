# ProofOrItDidntHappen
Repository for my blockchain project
🧱 Proof-of-Work Blockchain Simulation
📖 Описание проекта

Данный проект представляет собой реализацию блокчейна с механизмом консенсуса Proof-of-Work (PoW), интегрированного в распределённую P2P-сеть из нескольких узлов.
Цель проекта — продемонстрировать работу алгоритма PoW, процесс майнинга блоков, влияние уровня сложности (difficulty) на производительность и безопасность сети, а также устойчивость цепочки к подделке данных.

⚙️ Основные возможности

- Создание блоков с параметром nonce и вычислением хэша SHA-256;
- Механизм майнинга блока с учётом сложности (difficulty);
- Проверка целостности цепочки блоков;
- Обнаружение подделки данных и пересчёт («перемайнинг») цепочки;
- Симуляция обмена блоками между узлами P2P-сети;

🧩 Структура проекта
📂 project_root/
 ├── blockchainProg.py   # Реализация классов Block, Blockchain и Node
 ├── testPow.py          # Скрипт для симуляции работы сети и проверки защиты
 ├── README.md           # Описание проекта (этот файл)

🧠 Принцип работы

1. Майнер получает заголовок блока и подбирает значение nonce методом перебора.
2. Для каждого значения вычисляется хэш блока (SHA-256).
3. Если хэш начинается с требуемого количества нулей (difficulty), блок считается найденным.
4. Узел рассылает найденный блок другим участникам сети.
5. Остальные узлы проверяют корректность блока (хэш, индекс, предыдущий хэш, сложность).

🔒 Проверка защиты

В ходе эксперимента данные одного блока были изменены, после чего проведён «перемайнинг» начиная с этого блока.
Результат показал, что даже при difficulty = 5 подделка требует значительных вычислительных и временных затрат, что подтверждает надёжность механизма PoW.

⚡ Пример запуска
python testPow.py

Пример вывода:

Node_1 рассылает блок 'Транзакция A'
Node_2 рассылает блок 'Транзакция B'
Node_3 принял блок 'Транзакция A' от Node_1!
...
Целостность цепочки: False
Валидность блока: False
Среднее время на перемайнинг блока: 7.02 сек

🔬 Результаты

- При увеличении difficulty растёт время нахождения блока, но повышается безопасность.
- Подделка одного блока требует пересчёта всех последующих.
- Алгоритм PoW обеспечивает децентрализованный консенсус и защиту цепочки от подмены данных.

🧾 Выводы

Механизм Proof-of-Work делает блокчейн устойчивым к подделке за счёт вычислительной сложности нахождения блока.
Задача должна быть трудной для решения, но лёгкой для проверки, что гарантирует безопасность и согласованность данных между узлами.
